services:
  vytaan-submissions:
    build:
      context: .
      target: production
    ports:
      - "2000:2000"
    env_file:
      - .env
    environment:
      - PORT=0.0.0.0:2000
      - NODE_ENV=production
    volumes:
      - sqlite-data:/data
    restart: unless-stopped

  vytaan-bot:
    build:
      context: ./bot
    ports:
      - "8442:3000"
    env_file:
      - ./bot/.env
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  sqlite-data:
